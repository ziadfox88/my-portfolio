[build]
  command = "pip install -r requirements.txt && python manage.py collectstatic --noinput"
  publish = "staticfiles/"

[build.environment]
  # Pin to the same patch release
  PYTHON_VERSION = "3.8.17"
  DJANGO_SETTINGS_MODULE = "mysite.settings.production"
  DEBUG                 = "false"
